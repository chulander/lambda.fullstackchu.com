{
  "name": "authentication",
  "version": "0.0.1",
  "description": "post.authentication for fullstackchu.com",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf -R ./build && mkdir -p build",
    "build": "cp src/index.js ./build/index.js",
    "zip": "zip ./build.zip -r ./build ./node_modules",
    "bundle": "npm run clean && npm run build && npm run zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^8.4.0",
    "query-string": "^6.2.0"
  }
}
